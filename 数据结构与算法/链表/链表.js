/**
 * 实现链表的功能
 */

function Node(value) {
    this.value = value;
    this.next = null;
}

var a = new Node("a");
var b = new Node("b");
var c = new Node("c");

/**
 * 将链表输出
 * @param {*} root 
 */
function print(root) {
    if (!root) {
        return;
    }

}